#include <iostream>
#include "acutest.h"
#include "types.hpp"
#include "util_types.hpp"

// Create graph and test individual functions

// TEST_ASSERT that graph creation works,

// TEST_ASSERT that node creation inside graph works

// Continue with other tests


TEST_LIST = {

    { NULL, NULL }     // zeroed record marking the end of the list
};